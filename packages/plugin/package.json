{
  "name": "@html-to-figma/plugin",
  "version": "1.0.0",
  "scripts": {
    "dev": "BUILD_TARGET=code vite build --watch & BUILD_TARGET=ui vite build --watch",
    "build": "BUILD_TARGET=code vite build && BUILD_TARGET=ui vite build",
    "build:code": "BUILD_TARGET=code vite build",
    "build:ui": "BUILD_TARGET=ui vite build"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.95.0",
    "vite": "^5.3.0",
    "typescript": "^5.5.0"
  }
}
